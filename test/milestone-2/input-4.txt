program Math;
const
  PI == 3.14;
var
  a, b, c: real;
  flag: boolean;
begin
  a := -PI + 2.5 * (10.0 / 2.0) - 5.0;
  flag := not flag or (a <= b) and not (c == 0.0);
  repeat
    a := a - 0.1
  until a < 0.0
end.