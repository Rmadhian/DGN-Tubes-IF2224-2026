#include "interpreter_core.h"
#include <iostream>

// Mengimplementasikan fungsi eksekusi matematika/logika dari VirtualMachine

void VirtualMachine::executeOPR(int oprCode) {
    // TODO: Ambil nilai dari stack menggunakan pop(), hitung sesuai oprCode, 
    // lalu masukkan lagi hasilnya menggunakan push()
}